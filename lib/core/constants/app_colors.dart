import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette - kid-friendly, vibrant
  static const primary = Color(0xFF6C63FF); // Purple
  static const primaryLight = Color(0xFF9D97FF);
  static const primaryDark = Color(0xFF4A42DB);

  // Secondary
  static const secondary = Color(0xFFFF6584); // Pink
  static const secondaryLight = Color(0xFFFF8FA3);

  // Accent
  static const accent = Color(0xFFFFD166); // Yellow/Gold (coins)
  static const accentGreen = Color(0xFF06D6A0); // Green (healthy)
  static const accentRed = Color(0xFFEF476F); // Red (unhealthy)
  static const accentBlue = Color(0xFF118AB2); // Blue (info)

  // Category colors
  static const exercise = Color(0xFF06D6A0);
  static const study = Color(0xFF118AB2);
  static const chores = Color(0xFFFFD166);
  static const screenTime = Color(0xFFEF476F);
  static const creative = Color(0xFF9D4EDD);
  static const social = Color(0xFFFF6B6B);
  static const sleep = Color(0xFF073B4C);

  // Mood colors
  static const moodHappy = Color(0xFF06D6A0);
  static const moodNeutral = Color(0xFFFFD166);
  static const moodSad = Color(0xFF118AB2);
  static const moodSick = Color(0xFFEF476F);

  // Backgrounds
  static const background = Color(0xFFF8F9FE);
  static const surface = Colors.white;
  static const surfaceVariant = Color(0xFFF0F0F8);

  // Text
  static const textPrimary = Color(0xFF2D3142);
  static const textSecondary = Color(0xFF9C9CB4);
  static const textOnPrimary = Colors.white;
}
